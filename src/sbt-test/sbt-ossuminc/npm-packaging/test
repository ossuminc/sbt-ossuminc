# Verify npm package assembly (pure sbt, no npm binary needed).
# (sbt 2 target layout differs from sbt 1; checkPackageJson locates the output
# via NpmPackaging.Keys.npmOutputDir.value, so no hardcoded path checks here.)
> compile
> npmPrepare
> checkPackageJson
# Verify publishing configuration loads correctly
> checkPublishingConfig

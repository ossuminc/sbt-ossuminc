# Verify Homebrew formula generation
# (sbt 2 target layout is target/out/jvm/scala-<v>/<id>/... ; checkFormula uses
# target.value so it's path-agnostic — no hardcoded `exists` check needed.)
> compile
> app/homebrewGenerate
> app/checkFormula
